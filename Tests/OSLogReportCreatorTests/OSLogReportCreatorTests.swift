import XCTest
@testable import OSLogReportCreator

final class OSLogReportCreatorTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(OSLogReportCreator().text, "Hello, World!")
    }
}
